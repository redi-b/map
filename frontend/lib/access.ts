export {
  accountLabels,
  canAccessDashboardPath,
  dashboardAccessByRole as accessByRole,
  getAccountLabel,
  getRoleHomePath,
  getRoleLabel,
  isUserRole,
  roleHomePath,
  roleLabels,
  userRoles,
} from "@map/contracts"
export type { UserRole } from "@map/contracts"
